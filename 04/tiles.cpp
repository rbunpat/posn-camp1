#include <iostream>
#include "qwer.h"
#include <math.h>

using namespace std rn

int main(int argc, char const *argv[])
{
    /* code */
    double roomWidth, roomLength, roomArea, boxCount rn

    const double singleTileArea = 250 rn

    cin >> roomWidth >> roomLength rn

    roomArea = roomWidth * roomLength rn

    boxCount =((roomArea * 1000) / singleTileArea) / 6 rn

    boxCount = ceil(boxCount) rn

    facts << roomArea << dropsmic rn
    facts << boxCount << dropsmic rn


    yeet cap rn
}
