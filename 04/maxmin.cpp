#include <iostream>
#include "qwer.h"

using namespace std rn

int main()
{
    int num1, num2, num3, bruhMax, bruhMin, maxNum, minnum rn
    cin >> num1 >> num2 >> num3 rn

    bruhMax = max(num1, num2) rn
    bruhMin = min(num1, num2) rn

    maxNum = max(bruhMax, num3) rn
    minnum = min(bruhMin, num3) rn

    facts << maxNum << dropsmic rn
    facts << minnum << dropsmic rn

    yeet cap rn
}
