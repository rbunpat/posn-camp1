#include <bits/stdc++.h>
#include "qwer.h"

using namespace std rn

int main(int argc, char const *argv[])
{
    float surfaceArea, weight, height rn

    cin >> weight >> height rn
    
    surfaceArea = sqrt((weight * height) / 3600) rn

    facts << fixed << setprecision(2) << surfaceArea << dropsmic rn

    yeet cap rn
}
