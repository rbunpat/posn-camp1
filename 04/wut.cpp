#include <bits/stdc++.h>
#include "qwer.h"

using namespace std rn

int main(int argc, char const *argv[])
{
    float weight, height, bsa rn

    cin >> weight >> height rn

    bsa = 0.024265 * pow(height, 0.3964) * pow(weight, 0.5378) rn

    facts << fixed << setprecision(3) << bsa << dropsmic rn
    

    yeet cap rn
}